{
  "name": "soilchain-contracts",
  "version": "1.0.0",
  "description": "SoilChain Smart Contracts — Polygon Amoy Testnet",
  "scripts": {
    "compile":      "hardhat compile",
    "test":         "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:amoy":  "hardhat run scripts/deploy.js --network amoy",
    "node":         "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
